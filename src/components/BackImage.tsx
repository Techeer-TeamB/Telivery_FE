import React from "react";

const BackImage = (props: any) => {
  return (
    <div
      className="bg-[length:100vw_100vh]"
      style={{ backgroundImage: `url("../images/mainpage.png")` }}
    ></div>
  );
};

export default BackImage;
